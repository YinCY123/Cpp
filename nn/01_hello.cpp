#include <iostream>  // 引入"输入输出"工具库，才能用 std::cout 打印

// main 是程序的入口。运行程序时，电脑从这里开始执行。
int main() {
    // std::cout 是"标准输出"，<< 把右边的内容送出去显示在屏幕上。
    // "\n" 表示换行。
    std::cout << "我叫因此也\n";
    std::cout << "我要学会用C++写神经网络\n";

    // return 0 告诉操作系统："程序正常结束了"。
    return 0;
}
